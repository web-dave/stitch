import { DynDirDirective } from './dyn-dir.directive';

describe('DynDirDirective', () => {
  it('should create an instance', () => {
    const directive = new DynDirDirective();
    expect(directive).toBeTruthy();
  });
});
